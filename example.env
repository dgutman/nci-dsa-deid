DSAKEY=""
DSA_API_URL=""

# Optional: OAuth UNA callback URL when proxy headers are wrong (e.g. wsi-deid.cancer.gov).
# Must match the URL registered with your OAuth provider. Leave empty to use request headers.
# UNA_OAUTH_REDIRECT_URI=https://wsi-deid.cancer.gov/dsa/api/v1/oauth/una/callback